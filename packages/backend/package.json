{
  "name": "@lending-markets/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "yarn build && node dist/server.js",
    "dev": "node --import tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ethers": "^6.16.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^22",
    "tsx": "^4.21.0",
    "typescript": "5.7.3"
  }
}
