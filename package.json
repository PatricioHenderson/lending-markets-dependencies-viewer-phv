{
  "name": "@lending-markets/monorepo",
  "version": "1.0.0",
  "packageManager": "yarn@1.22.19",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "yarn workspaces run lint",
    "build": "yarn workspaces run build",
    "dev": "yarn workspace @lending-markets/backend dev & yarn workspace @lending-markets/app dev & wait",
    "start": "yarn workspace @lending-markets/backend start & yarn workspace @lending-markets/app start & wait"
  }
}
